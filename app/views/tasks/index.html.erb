<h1>Listing tasks</h1>
<p>
<%= link_to 'New task', new_task_path %>
</p>
<ul id="tasks">
  <%= render @tasks %>
</ul>
<h3>Add a new note:</h3>
<%= form_for(@note, remote: true) do |f| %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.submit %>
<% end %>